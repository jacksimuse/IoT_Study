- 패스트포워드(fast-forward)
: 현재 작업 중인 브랜치가 있고, 다른 브랜치의 내용을 머지(합병)하는 경우 발생하는 특별한 머지(merge).

- 인덱스(index)
: 변경 내역이 포함된 파일들의 모음. Git 저장소에 가기 전에 기록되는 공간을 일컫는 말.

- staged
: Git의 index에 들어간 상태.

- unstaged
: Git의 index에 안들어간 상태.